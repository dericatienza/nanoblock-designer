import { GridHighlighterDirective } from './grid-highlighter.directive';

describe('GridHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new GridHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
