import { JsonLoaderDirective } from './json-loader.directive';

describe('JsonLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new JsonLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
