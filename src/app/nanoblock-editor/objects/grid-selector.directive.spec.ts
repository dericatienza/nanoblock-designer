import { GridSelectorDirective } from './grid-selector.directive';

describe('GridSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new GridSelectorDirective();
    expect(directive).toBeTruthy();
  });
});
