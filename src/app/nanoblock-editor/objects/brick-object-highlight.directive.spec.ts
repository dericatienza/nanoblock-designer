import { BrickObjectHighlightDirective } from './brick-object-highlight.directive';

describe('BrickObjectHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BrickObjectHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
